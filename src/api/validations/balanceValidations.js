// src/routes/balanceRoutes.js

// import express from 'express';
// import { updateBalance } from '../controllers/balanceController.js';
// import authenticateToken from "../middlewares/authenticateToken.js";  // Assuming you have authentication middleware

// const router = express.Router();

// router.post('/user/:id/balance', authenticateToken, updateBalance);

// export default router;
